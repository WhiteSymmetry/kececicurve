# pyproject.toml

[build-system]
requires = ["setuptools>=84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kececicurve"
version = "0.1.8"
dynamic = ["keywords", "maintainers"]
license = "AGPL-3.0-or-later"  # SPDX ID

authors = [
    { name="Mehmet Keçeci", email="enfo@tuta.io" },
]
description = "kececicurve, Keçeci Curve, Keçeci Eğrisi Modülü"
readme = "README.md"
requires-python = ">=3.11"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Visualization",
]

dependencies = ["numpy", "matplotlib"]

[project.optional-dependencies]
test = ["pytest", "pytest-cov", "pytest-mock", "ruff", "numpy", "matplotlib", "scipy", "chess", "kha256"]

[tool.setuptools.packages.find]
where = ["."]

[project.urls]
Homepage = "https://github.com/WhiteSymmetry/kececicurve"
Repository = "https://github.com/WhiteSymmetry/kececicurve"

[dependency-groups]
build = [
    "setuptools (>=84.0.0)",
    "cython (>=3.2.4)"
]
