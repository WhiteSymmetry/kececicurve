{% set version = "0.1.7" %}

package:
  name: kececicurve
  version: {{ version }}

source:
  path: .  # Derlemeyi bu meta.yaml dosyasının bulunduğu dizinden yap
  url: 
  blake2b-256: 

channels:
  - conda-forge

build:
  noarch: python  # Platformdan bağımsız bir paket oluşturur
  #script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed"
  script: "{{ PYTHON }} -m pip install . -vv"
  number: 0
  entry_points:
    - kececicurve = kececicurve.__main__:main

requirements:
  host:
    - python {{ python }}
    - numpy
    - pip
    - setuptools
    # Other build tools
  run:
    - python >=3.11
    - kha256
    - matplotlib
    - numpy
    - Pillow
    - scipy
  build:
    - python >=3.11
    - pip
    - setuptools
    - numpy

test:
  requires:
    - python >=3.11
    - kha256
    - matplotlib
    - numpy
    - Pillow
    - scipy
  imports:
    - kha256
    - matplotlib
    - numpy
    - Pillow
    - scipy

about:
  home: https://github.com/WhiteSymmetry/kececicurve
  license: AGPL-3.0-or-later
  summary: 'kececicurve, Keçeci Curve, Keçeci Eğrisi Modülü'
  description: |
    kececicurve, Keçeci Curve, Keçeci Eğrisi Modülü
  dev_url: https://github.com/WhiteSymmetry/kececicurve
  doc_url: https://github.com/WhiteSymmetry/kececicurve
  doc_source_url: https://github.com/WhiteSymmetry/kececicurve/blob/main/README.md

